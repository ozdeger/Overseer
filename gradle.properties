org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.caching=true
# Config cache left off — some IntelliJ Platform Gradle tasks aren't fully compatible on first run.
org.gradle.configuration-cache=false
# Don't bundle the Kotlin stdlib in the plugin; the IDE provides it (JetBrains-recommended).
kotlin.stdlib.default.dependency=false

